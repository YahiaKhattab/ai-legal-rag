[pytest]
; Points pytest at the teammates' AI project source code so
; `import legal_rag...` works without installing the package.
; Adjust this path if you place this folder somewhere else relative
; to the ai-legal-rag project.
pythonpath = ../ai-legal-rag-main/src
addopts = -ra
testpaths = .
